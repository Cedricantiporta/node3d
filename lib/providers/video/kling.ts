import { createStubVideoProvider } from "./factory";

// TODO: implement — see factory.ts for the intended shape.
export const KlingProvider = createStubVideoProvider(
  "kling",
  "Kling",
  "Submit prompts directly to Kling and poll for the rendered video."
);
