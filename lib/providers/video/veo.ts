import { createStubVideoProvider } from "./factory";

// TODO: implement — see factory.ts for the intended shape.
export const VeoProvider = createStubVideoProvider(
  "veo",
  "Veo",
  "Submit prompts directly to Google Veo and poll for the rendered video."
);
