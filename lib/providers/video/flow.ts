import { createStubVideoProvider } from "./factory";

// TODO: implement — see factory.ts for the intended shape.
export const FlowProvider = createStubVideoProvider(
  "flow",
  "Google Flow",
  "Submit prompts directly to Google Flow and poll for the rendered video."
);
