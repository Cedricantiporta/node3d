import { createStubVideoProvider } from "./factory";

// TODO: implement — see factory.ts for the intended shape.
export const RunwayProvider = createStubVideoProvider(
  "runway",
  "Runway",
  "Submit prompts directly to Runway and poll for the rendered video."
);
