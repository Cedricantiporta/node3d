import { createStubVideoProvider } from "./factory";

// TODO: implement — see factory.ts for the intended shape.
export const HailuoProvider = createStubVideoProvider(
  "hailuo",
  "Hailuo",
  "Submit prompts directly to Hailuo (MiniMax) and poll for the rendered video."
);
