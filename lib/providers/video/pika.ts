import { createStubVideoProvider } from "./factory";

// TODO: implement — see factory.ts for the intended shape.
export const PikaProvider = createStubVideoProvider(
  "pika",
  "Pika",
  "Submit prompts directly to Pika and poll for the rendered video."
);
